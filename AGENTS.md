# Agent rules

`README.md` owns Fableplan's current routing, supported Claude Code version,
fallback behavior, and verification commands. Verify the installed Claude Code
bundle and current official docs before changing model names or hook assumptions;
old session notes are not evidence.

Show the real tradeoffs before asking Tyler to choose. Keep invocation-scoped
environment changes inside Fableplan and preserve the user's ordinary Claude
configuration.
